syntax = "proto3";

package bunsan.broker.worker;

import "bunsan/broker/common.proto";

message ConnectionParameters {
    string identifier = 1;
    Connection connection = 2;
    Credentials credentials = 3;
    Constraints constraints = 4;
}
