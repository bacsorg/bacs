add_subdirectory(common)
if(ENABLE_CURL)
    add_subdirectory(external)
endif()
add_subdirectory(problem)
add_subdirectory(archive)
bunsan_enable_testing()
if(ENABLE_WEB)
    add_subdirectory(statement_provider)
endif()
